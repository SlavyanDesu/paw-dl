{
  "name": "paw-dl",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build ./src/index.ts --compile --outfile paw-dl",
    "format": "bunx prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.9.6"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "dependencies": {
    "p-limit": "^7.3.2",
    "p-retry": "^8.0.1",
    "sanitize-filename": "^1.6.4",
    "zod": "^4.5.4"
  }
}
